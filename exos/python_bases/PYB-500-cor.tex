\question{} 
\begin{pyverbatim}
from math import sqrt 

def racine(n):
    """sqrt(n), entier si n est un carré parfait"""
    s = sqrt(n)
    if s==int(s):
        return int(s)
    else:
        return s
\end{pyverbatim}

\end{document}