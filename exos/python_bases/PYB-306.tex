\question\ Écrire une fonction \pyv{appartient(a,c,r)} prenant en argument 
\begin{itemize}
  \item un couple de nombres \pyv{a} ; 
  \item un couple de nombres \pyv{c} ; 
  \item un nombre positif \pyv{r} ;
\end{itemize}
et renvoyant la valeur de vérité de \og le point de coordonnées \pyv{a} est dans le disque fermé de centre de coordonnées \pyv{c} et de rayon \pyv{r} \fg. 