\question{} \'Ecrire une fonction \pyv{moy_extr(L)} qui prend en argument une liste \pyv{L} non vide et renvoie en sortie la moyenne du premier et du dernier élément de \pyv{L}.

\question{}On cherche à écrire une fonction prenant en argument une liste d'entiers (non vide) et incrémentant de $1$ le premier élément de cette liste.
\begin{enumerate}[label=\emph{\alph*)}]
  \item \'Ecrire une telle fonction \pyv{incr_sans_effet_de_bord}, qui ne modifie pas la liste initiale et renvoie en sortie une nouvelle liste.
  \item \'Ecrire une telle fonction \pyv{incr_avec_effet_de_bord}, qui modifie la liste initiale et ne renvoie rien en sortie (ponctuer par un \pyv{return None}).
\end{enumerate}
