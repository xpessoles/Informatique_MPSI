Écrire une fonction \pyv{somme_chiffres(n)} qui prend en argument un entier naturel \pyv{n} et qui renvoie la somme des chiffres de \pyv{n} (écrit en base dix). 

On pourra utiliser toutes les fonctions de conversion présentes dans \python{}, mais la fonction rendue devra comporter au moins une boucle non triviale.