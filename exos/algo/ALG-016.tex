Pour les deux questions suivantes, les fonctions \pyv{max} et \pyv{min} ne sont pas autorisées.

\question{Écrire une fonction \pyv{chercheMax(L)} qui prend comme argument une liste \pyv{L} d'entiers \pyv{int} et qui renvoie le plus grand élément de la liste.}

\question{Écrire une fonction \pyv{chercheMaxIndice(L)} qui prend comme argument une liste \pyv{L} d'entiers \pyv{int} et qui renvoie l'indice du plus grand élément de la liste.}