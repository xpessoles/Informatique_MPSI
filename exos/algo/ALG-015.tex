\question{Écrire une fonction \pyv{ajouteUnFor(L)} qui prend comme argument une liste \pyv{L} de flottants et qui ajoute 1 à chaque élément de la liste. On utilisera une boucle \textbf{for}.}

\question{Écrire une fonction \pyv{ajouteUnWhile(L)} qui prend comme argument une liste \pyv{L} de flottants et qui ajoute 1 à chaque élément de la liste. On utilisera une boucle \textbf{while}.}

\question{Expliquer pourquoi il n'est pas indispensable que la fonction renvoie la liste modifiée.}