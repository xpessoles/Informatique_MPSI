\begin{xxpy}~\\
\vspace{-1cm}
\begin{pyverbatim}
def fonction(x) :
    y=1.1
    i=0
    while x!=y and i <10:
        x=x+0.1
        i=i+1
    return i
\end{pyverbatim}
\end{xxpy}

\question{On exécute l'instruction \pyv{fonction(0.1)}. À chaque itération, donner la valeur de $i$ et de $x$.}

\question{On exécute l'instruction \pyv{fonction(0.3)}. À chaque itération, donner la valeur de $i$ et de $x$.}